import { useEffect } from "react"

const Game = () => {
    useEffect(() => {})
    return (
        <>
            <canvas></canvas>
        </>
    )
}

export default Game

function setup() {
    const canvas = document.querySelector(canvas)
}
